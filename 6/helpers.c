#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <stdbool.h>
#include <math.h>
#include "helpers.h"

//char *splitlinetoint(FILE *fp, const char *delim, int arrlen, char *nums) {
//    char line[arrlen];
//    char *token;
//    int i = 0;
//    int nums[arrlen];
//
//    fgets(line, arrlen, fp);
//    token = strtok(line, delim);
//    while (token != NULL) {
//        nums[i] = atoi(token);
//        token = strtok(NULL, ", ");
//        i++;
//    };
//    entries = &i;
//    return (nums);
//}
