#ifndef HELPERS_H_
#define HELPERS_H_

#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int *splitlinetoint(FILE *fp, const char *delim, int arrlen, int *entries);
int testfun(int a, int b);

#endif
